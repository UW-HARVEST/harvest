//! Build-script renderer scaffolding.
//!
//! This module is reserved for the deterministic `build.rs` template emitted
//! from a [`BuildConfigIR`]. It is empty for now -- the crate layout reserves
//! the slot so later code that fills it in does not need to restructure
//! `build_config`'s public surface.
