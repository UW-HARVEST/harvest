{
  "name": "project_spec",
  "schema": {
    "type": "object",
    "properties": {
      "targets": {
        "type": "array",
        "description": "Ordered list of targets in compile/link order.",
        "items": {
          "type": "object",
          "properties": {
            "artifact": {
              "type": "string"
            },
            "kind": {
              "type": "string",
              "enum": ["Library", "Executable"]
            },
            "sources": {
              "type": "array",
              "items": {
                "type": "string",
                "pattern": ".*\\.(c|h)$"
              }
            }
          },
          "required": ["artifact", "kind", "sources"]
        }
      }
    },
    "required": ["targets"]
  }
}
